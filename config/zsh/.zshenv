# ~/.config/zsh/.zshenv
# Loaded FIRST for every zsh session

# Set ZDOTDIR if not already set (for XDG compliance)
export ZDOTDIR="${ZDOTDIR:-$HOME/.config/zsh}"

# Basic environment
export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8
export TERM=kitty