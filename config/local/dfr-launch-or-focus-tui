#!/usr/bin/env bash
# Launch or focus a TUI application
set -euo pipefail

app="$1"
shift

if pgrep -x "$app" &>/dev/null; then
    niri msg action focus-window --app-id "$app" 2>/dev/null || true
else
    exec "$app" "$@" &
fi
